import { View } from "react-native";

export default function HorizontalLine(): JSX.Element {
    return (
        <View
            style={{
                height: 2,
                backgroundColor: "#ccc",
            }}
        />
    );
}
